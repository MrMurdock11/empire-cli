import { Keys } from "./@store-name@.keys";
import { Actions } from "./@store-name@.actions.type";
import { initState, State } from "./@store-name@.state";

export const @reducer-name@ = (state = initState, action: Actions): State => {
	switch(action.type) {
		// case Keys.YOUR_KEY:
		// 	return {...state};
		default:
			return state;
	}
}
